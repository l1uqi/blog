<% if (page.total > 1){ %>
  <nav class="pagination flex items-center justify-center gap-3 py-12 text-sm">
      <%- paginator({
          prev_text: "Prev",
          next_text: "Next",
          prev_next: true,
          end_size: 1,
          mid_size: 2,
      }) %>
  </nav>
<% } %>
